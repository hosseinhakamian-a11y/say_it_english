# 🌴 نخلستان معنا - حافظه پروژه (MANA_MEMORY.md)

## 📅 تاریخ آخرین بروزرسانی: ۲۰۲۵-۱۲-۲۷

### 📋 وضعیت فعلی پروژه
پروژه با موفقیت در محیط لوکال (ویندوز) روی پورت **۵۰۰۰** پیکربندی و اجرا شد.

### ✅ تغییرات اعمال شده (امروز)
1.  **سازگاری با ویندوز**:
    - اسکریپت `dev` در `package.json` به `cross-env` مجهز شد تا متغیرهای محیطی در ویندوز به درستی ست شوند.
    - تنظیمات `httpServer.listen` در `server/index.ts` برای رفع خطای پورت در ویندوز اصلاح شد (حذف `reusePort` و تغییر `host`).
2.  **تغییر استراتژی دیتابیس (فوری)**:
    - به دلیل نبود PostgreSQL محلی، پروژه به صورت موقت به **MemStorage** (ذخیره‌سازی در حافظه) تغییر یافت تا فرآیند توسعه و مشاهده رابط کاربری متوقف نشود.
    - فایل `server/storage.ts` بازنویسی شد تا از اینترفیس `IStorage` پشتیبانی کند.
3.  **اجرای موفق**:
    - پروژه در حال حاضر روی آدرس `http://localhost:5000` در حال اجراست.

### 🚀 گام‌های بعدی برای فردا
- [ ] **تصمیم‌گیری در مورد دیتابیس**: بازگشت به PostgreSQL (در صورت نصب دیتابیس توسط کاربر) یا استفاده از SQLite برای پایداری داده‌ها در محیط لوکال.
- [ ] **بررسی بخش‌های تخصصی**: شروع کار روی بخش‌های مربوط به "آموزش تخصصی زبان انگلیسی" طبق درخواست پروژه.
- [ ] **هم‌راستایی با پرامپت v6.0**: اعمال استانداردهای طراحی و عملکردی نخلستان معنا در این پروژه.

---
*این متن به عنوان "حافظه زنده" پروژه عمل می‌کند تا در هر جلسه از آخرین وضعیت آگاه باشیم.*
