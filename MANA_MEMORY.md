# 🌴 Say It English - حافظه پروژه (MANA_MEMORY.md)

این فایل "مغز متفکر" پروژه است. هر زمان که نیاز به یادآوری معماری، راه حل‌های قبلی یا وضعیت سیستم داشتید، ابتدا به اینجا مراجعه کنید.

## 📅 آخرین بروزرسانی: ۲۳ بهمن ۱۴۰۴ (2026-02-12) - ساعت ۱۴:۲۰ تهران

---

## � فهرست مستندات مرجع (Reference Documentation)
برای جزئیات فنی دقیق، به فایل‌های زیر در پوشه `.agent/patterns` مراجعه کنید:

1.  **🐞 راهنمای عیب‌یاابی (Troubleshooting):**
    - 📂 آدرس: `.agent/patterns/TROUBLESHOOTING_GUIDE.md`
    - **کاربرد:** وقتی محتوا لود نمی‌شود، پروفایل ذخیره نمی‌شود، یا دیتابیس وصل نیست.

2.  **📲 راهنمای پیاده‌سازی OTP (پیامک):**
    - 📂 آدرس: `.agent/patterns/OTP_IMPLEMENTATION_GUIDE.md`
    - **کاربرد:** استاندارد صحیح اینپوت‌های کد تایید (بدون باگ پرش و فاصله).

---

## 🚀 دستاوردهای کلیدی اخیر (جلسه ۲۳ بهمن - Stabilization & DB Fixes) ✅

### 1. حل مشکل ناپدید شدن محتوا (Database Region Migration) 🗄️
- **مشکل:** دیتابیس از `eu-central-1` (آلمان) به `eu-north-1` (سوئد) منتقل شده بود ولی سرور لوکال به آدرس قبلی وصل می‌شد.
- **راه حل:** آپدیت `.env.local` با آدرس جدید و **ریستارت سرور**.
- **وضعیت فعلی:** ۱۰ ویدیو، ۳ مقاله و ۵ کاربر همگی بازیابی شدند.

### 2. بازنویسی کامل سیستم OTP (ورود پیامکی) 📱
- **مشکل:** پرش نشانگر، پاک نشدن اعداد، و خطای "کد باید ۶ رقم باشد".
- **راه حل:**
    - استفاده از **Local State Array** (`['', '', ...]`) به جای کنترل مستقیم فرم.
    - مخفی کردن `Space` در UI (`value={char === " " ? "" : char}`).
    - فونت فارسی: استفاده از **Vazirmatn** برای نمایش اعداد توپر و زیبا.
- **فایل کلیدی:** `client/src/pages/Auth.tsx`

### 3. تکمیل API پروفایل کاربری 👤
- **مشکل:** خطای 404 هنگام ویرایش نام یا تغییر رمز عبور.
- **راه حل:** افزودن روت‌های `PATCH /api/profile` و `POST /api/profile/password` به `api/index.ts`.

---

## 🏗️ معماری سیستم (System Architecture)

- **Frontend:** React + Vite + TailwindCSS (Glassmorphism UI)
- **Backend:** Node.js (Vercel Serverless Functions) - Monolithic entry at `api/index.ts`
- **Database:** Supabase PostgreSQL (Region: `eu-north-1`)
- **Hosting:** Vercel
- **State Management:** React Query (Server State) + React Context (Auth)

---

## 📊 وضعیت فعلی سیستم (System Health)

| کامپوننت | وضعیت | توضیحات |
| :--- | :--- | :--- |
| **Auth (OTP)** | 🟢 عالی | فونت فارسی، بدون باگ تایپ، تایمر فعال |
| **Database** | 🟢 عالی | وصل به `aws-1-eu-north-1`، داده‌ها محفوظ |
| **Profile** | 🟢 عالی | قابلیت ویرایش نام و تغییر رمز عبور فعال |
| **Content** | 🟢 عالی | نمایش صحیح ویدیوها و مقالات (فریم‌ورک Freemium) |
| **Deploy** | 🟢 نیاز به بررسی | آخرین کامیت پوش شده، نیاز به ردیپلوی در Vercel |

---

## 🎯 نقشه راه آینده (Next Steps)

### اولویت ۱: داشبورد کاربر
- [ ] نمایش لیست دوره‌های خریداری شده در `/dashboard`.
- [ ] نمایش نمودار پیشرفت یادگیری.

### اولویت ۲: سیستم PDF ساز
- [ ] دکمه "دانلود PDF درس" در صفحه جزئیات ویدیو.

---

## 💸 اطلاعات مالی پروژه
- **آدرس ولت (BEP20):** `0x2ca84105e9e3f3a91f0385acbd497923d743a342`
