# 🌴 Say It English - حافظه پروژه (MANA_MEMORY.md)

## 📅 تاریخ آخرین بروزرسانی: ۱۹ بهمن ۱۴۰۴ (2026-02-08) - ساعت ۱۵:۱۰ تهران

---

## 🚀 دستاوردهای کلیدی اخیر (جلسه فعلی)

### 1. راه‌اندازی سیستم وبلاگ حرفه‌ای (Blog Engine) ✅
- **ساختار دیتابیس:** افزودن نوع `article` به جدول `content` به همراه فیلدهای `slug`, `author`, `tags`, `body`.
- **صفحات فرانت‌اند:**
    - `Blog.tsx`: نمایش لیست مقالات با طراحیGrid کارت، فیلتر و انیمیشن.
    - `ArticleDetail.tsx`: صفحه مطالعه مقاله با طراحی **Parallax Hero**، نوار پیشرفت مطالعه، سایدبار پیشنهاد مطالب و تایپوگرافی بهینه فارسی.
- **محتوا:** بارگذاری ۳ مقاله تخصصی آیلتس و مکالمه (توسط `update_articles.sql`).

### 2. زیرساخت سئو (SEO Infrastructure) ✅
- **کامپوننت هوشمند:** ساخت `components/SEO.tsx` که به صورت خودکار متاتگ‌ها (Title, Description, OG Tags, Twitter Cards) را مدیریت می‌کند.
- **Structured Data (JSON-LD):** اضافه شدن اسکیما مارک‌آپ برای مقالات (`Article Schema`) جهت نمایش بهتر در گوگل.

### 3. معماری یکپارچه بک‌اند (Monolithic API) ✅
- **حل مشکل Vercel Limits:** تمام 12 فایل API جداگانه (`login.ts`, `content.ts`, ...) در یک فایل واحد **`api/index.ts`** ادغام شدند.
- **مزایا:**
    - کاهش Cold Start (گرم ماندن تابع).
    - مدیریت بهتر اتصال دیتابیس (Connection Pooling).
    - رفع خطای محدودیت تعداد Function در پلن رایگان Vercel.

### 4. اصلاحات امنیتی و پایداری ✅
- **رفع باگ Variable Shadowing:** اصلاح نام‌گذاری متغیر `res` در بلوک‌های API که باعث کرش کردن سرور می‌شد.
- **Cookie Security:** اطمینان از فلگ‌های `HttpOnly`, `Secure`, `SameSite: Lax`.

---

## 📊 وضعیت فعلی سیستم (System Health)

- **Frontend:** React + Vite + TailwindCSS (Glassmorphism UI).
- **Backend:** Node.js (Vercel Serverless Function) - Single Entry Point.
- **Database:** Supabase (PostgreSQL) - Tables: `users`, `content`, `reviews`, `bookings`, `payments`, `time_slots`.
- **Deploy:** Vercel (Status: **Healthy** 🟢).

---

## 🎯 نقشه راه آینده (Next Session Roadmap)

### اولویت ۱: درآمدزایی (Monetization)
- [ ] **صفحه قیمت‌گذاری (Pricing Page):** طراحی پلن‌های اشتراک (ماهانه/سالیانه).
- [ ] **درگاه پرداخت:** اتصال به درگاه واقعی (زرین‌پال/نکست‌پی) یا شبیه‌ساز دقیق‌تر.

### اولویت ۲: سیستم رزرو (Booking System)
- [ ] **تکمیل API:** اتصال API رزرو به فرانت‌اند.
- [ ] **تقویم تعاملی:** نمایش اسلات‌های خالی به کاربر و رزرو آنی.

### اولویت ۳: داشبورد کاربر (User Dashboard)
- [ ] نمایش دوره‌های خریداری شده.
- [ ] نمایش وضعیت اشتراک.
- [ ] مدیریت رزروهای انجام شده.

---

## 🔧 راهنمای توسعه‌دهنده (Developer Guide)

### افزودن مقاله جدید
1. مقاله را در دیتابیس `content` با تایپ `article` اضافه کنید.
2. متن مقاله (HTML) را در ستون `body` قرار دهید.
3. برای عکس کاور از `thumbnail_url` استفاده کنید.

### دیپلوی در Vercel
با توجه به معماری Monolith، هیچ کانفیگ خاصی لازم نیست. فقط `git push` کنید. تمام درخواست‌های `/api/*` به `api/index.ts` هدایت می‌شوند.

---

## 💸 اطلاعات مالی پروژه
- **آدرس ولت (BEP20):** `0x2ca84105e9e3f3a91f0385acbd497923d743a342`
