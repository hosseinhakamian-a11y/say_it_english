---
description: راهنمای دیپلوی پروژه در Netlify با دیتابیس Supabase
---

# 🚀 راهنمای دیپلوی پروژه در Netlify با Supabase

این راهنما برای افرادی است که کدنویسی بلد نیستند و می‌خواهند پروژه خود را به صورت آنلاین منتشر کنند.

---

## 📋 پیش‌نیازها

قبل از شروع، این موارد را آماده کنید:
- ✅ یک حساب کاربری در [GitHub](https://github.com) (رایگان)
- ✅ یک حساب کاربری در [Netlify](https://netlify.com) (رایگان)
- ✅ یک حساب کاربری در [Supabase](https://supabase.com) (رایگان)
- ✅ پروژه شما باید در GitHub آپلود شده باشد

---

## مرحله ۱: راه‌اندازی دیتابیس در Supabase

### ۱.۱ ساخت پروژه جدید در Supabase

1. به وب‌سایت [supabase.com](https://supabase.com) بروید
2. روی دکمه **"Start your project"** کلیک کنید
3. با حساب GitHub خود وارد شوید
4. روی **"New Project"** کلیک کنید
5. اطلاعات زیر را وارد کنید:
   - **Name**: یک نام برای پروژه (مثلاً `say-it-english-db`)
   - **Database Password**: یک رمز عبور قوی (حتماً آن را یادداشت کنید!)
   - **Region**: نزدیک‌ترین منطقه به ایران را انتخاب کنید (مثلاً `Southeast Asia (Singapore)`)
   - **Pricing Plan**: گزینه **Free** را انتخاب کنید
6. روی **"Create new project"** کلیک کنید
7. صبر کنید تا پروژه ساخته شود (حدود ۲ دقیقه)

### ۱.۲ دریافت اطلاعات اتصال به دیتابیس

1. بعد از ساخته شدن پروژه، از منوی سمت چپ روی **"Project Settings"** (آیکون چرخ دنده) کلیک کنید
2. از منوی باز شده، روی **"Database"** کلیک کنید
3. به پایین صفحه بروید تا بخش **"Connection string"** را ببینید
4. روی تب **"URI"** کلیک کنید
5. گزینه **"Display connection pooler"** را خاموش کنید
6. متن کامل را کپی کنید (شبیه این است):
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxxxxxxxxx.supabase.co:5432/postgres
   ```
7. در این متن، کلمه `[YOUR-PASSWORD]` را با رمز عبوری که در مرحله قبل ساختید جایگزین کنید
8. این متن کامل را در یک فایل نوت‌پد ذخیره کنید (به آن نیاز خواهیم داشت)

---

## مرحله ۲: آپلود کد در GitHub

### ۲.۱ بررسی وضعیت GitHub

اگر پروژه شما هنوز در GitHub نیست، این مراحل را دنبال کنید:

1. به [github.com](https://github.com) بروید و وارد حساب خود شوید
2. روی دکمه **"+"** در گوشه بالا سمت راست کلیک کنید
3. **"New repository"** را انتخاب کنید
4. نام repository را وارد کنید (مثلاً `say-it-english`)
5. گزینه **"Public"** یا **"Private"** را انتخاب کنید
6. روی **"Create repository"** کلیک کنید

### ۲.۲ آپلود کد به GitHub

اگر Git را بلد نیستید، از طریق VS Code این کار را انجام دهید:

1. پوشه پروژه را در VS Code باز کنید
2. از منوی سمت چپ، روی آیکون **Source Control** (شبیه شاخه درخت) کلیک کنید
3. روی **"Publish to GitHub"** کلیک کنید
4. نام repository را انتخاب کنید
5. صبر کنید تا کد آپلود شود

---

## مرحله ۳: ساخت فایل‌های مورد نیاز برای Netlify

### ۳.۱ ساخت فایل netlify.toml

این فایل به Netlify می‌گوید چگونه پروژه شما را بیلد و اجرا کند.

**این فایل باید در ریشه پروژه (کنار package.json) ساخته شود.**

محتوای فایل:
```toml
[build]
  command = "npm run build"
  publish = "dist/public"
  functions = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### ۳.۲ ساخت فایل .nvmrc

این فایل نسخه Node.js را مشخص می‌کند.

**این فایل باید در ریشه پروژه ساخته شود.**

محتوای فایل:
```
20
```

---

## مرحله ۴: تنظیم کد برای Netlify Functions

### ۴.۱ ساخت فایل Netlify Function Handler

این فایل باید در مسیر `server/netlify.ts` ساخته شود.

محتوای فایل:
```typescript
import serverless from "serverless-http";
import express from "express";
import { registerRoutes } from "./routes";

const app = express();

app.use(express.json());
app.use(express.urlencoded({ extended: false }));

// Register API routes
registerRoutes(null as any, app);

export const handler = serverless(app);
```

### ۴.۲ نصب پکیج serverless-http

این پکیج Express را با Netlify Functions سازگار می‌کند.

**دستور زیر را در ترمینال اجرا کنید:**
```bash
npm install serverless-http
```

### ۴.۳ به‌روزرسانی package.json

اسکریپت build باید به‌روز شود تا برای Netlify مناسب باشد.

در فایل `package.json`، بخش scripts را به این صورت تغییر دهید:
```json
"scripts": {
  "dev": "cross-env NODE_ENV=development tsx server/index.ts",
  "build": "tsx script/build.ts && npm run build:server",
  "build:server": "esbuild server/netlify.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:pg-native",
  "start": "NODE_ENV=production node dist/index.cjs",
  "check": "tsc",
  "db:push": "drizzle-kit push"
}
```

---

## مرحله ۵: دیپلوی در Netlify

### ۵.۱ اتصال GitHub به Netlify

1. به [netlify.com](https://netlify.com) بروید
2. روی **"Sign up"** یا **"Log in"** کلیک کنید
3. گزینه **"GitHub"** را انتخاب کنید و اجازه دسترسی بدهید
4. بعد از ورود، روی **"Add new site"** کلیک کنید
5. گزینه **"Import an existing project"** را انتخاب کنید
6. **"GitHub"** را انتخاب کنید
7. repository پروژه خود را پیدا کنید و روی آن کلیک کنید

### ۵.۲ تنظیمات Build

Netlify به صورت خودکار تنظیمات را از فایل `netlify.toml` می‌خواند، اما بررسی کنید:

- **Build command**: `npm run build`
- **Publish directory**: `dist/public`
- **Functions directory**: `dist`

### ۵.۳ تنظیم Environment Variables

این مهم‌ترین بخش است! باید اطلاعات دیتابیس را به Netlify بدهید:

1. قبل از کلیک روی **"Deploy"**، روی **"Show advanced"** کلیک کنید
2. روی **"New variable"** کلیک کنید
3. متغیرهای زیر را اضافه کنید:

   **Key**: `DATABASE_URL`  
   **Value**: رشته اتصال که از Supabase کپی کردید (مثلاً `postgresql://postgres:yourpassword@db.xxx.supabase.co:5432/postgres`)

   **Key**: `NODE_ENV`  
   **Value**: `production`

4. روی **"Deploy site"** کلیک کنید

### ۵.۴ صبر برای Build

- Netlify شروع به build کردن پروژه می‌کند
- این فرآیند ممکن است ۲-۵ دقیقه طول بکشد
- می‌توانید لاگ‌ها را ببینید تا مطمئن شوید همه چیز درست پیش می‌رود

---

## مرحله ۶: اجرای Migration دیتابیس

بعد از دیپلوی موفق، باید جداول دیتابیس را بسازید:

### ۶.۱ نصب Drizzle Kit (اگر نصب نیست)

```bash
npm install -g drizzle-kit
```

### ۶.۲ تنظیم متغیر محیطی در کامپیوتر خودتان

**در Windows:**
```powershell
$env:DATABASE_URL="postgresql://postgres:yourpassword@db.xxx.supabase.co:5432/postgres"
```

**در Mac/Linux:**
```bash
export DATABASE_URL="postgresql://postgres:yourpassword@db.xxx.supabase.co:5432/postgres"
```

### ۶.۳ اجرای Migration

```bash
npm run db:push
```

این دستور جداول مورد نیاز را در دیتابیس Supabase شما می‌سازد.

---

## مرحله ۷: تست و بررسی

### ۷.۱ باز کردن سایت

1. در داشبورد Netlify، لینک سایت شما نمایش داده می‌شود (مثلاً `https://your-site-name.netlify.app`)
2. روی لینک کلیک کنید
3. سایت شما باید باز شود!

### ۷.۲ بررسی عملکرد

- صفحات مختلف را باز کنید
- اگر سایت شما فرم دارد، آن را تست کنید
- اگر خطایی دیدید، به مرحله بعد بروید

### ۷.۳ بررسی لاگ‌ها (در صورت خطا)

1. در داشبورد Netlify، روی **"Functions"** کلیک کنید
2. لاگ‌های خطا را بررسی کنید
3. اگر خطای مربوط به DATABASE_URL دیدید، دوباره Environment Variables را چک کنید

---

## مرحله ۸: تنظیمات اضافی (اختیاری)

### ۸.۱ تغییر نام دامنه

1. در داشبورد Netlify، روی **"Domain settings"** کلیک کنید
2. می‌توانید یک نام دلخواه انتخاب کنید (مثلاً `say-it-english.netlify.app`)
3. یا یک دامنه شخصی متصل کنید

### ۸.۲ فعال‌سازی HTTPS

Netlify به صورت خودکار HTTPS را فعال می‌کند، اما بررسی کنید:

1. در **"Domain settings"** روی **"HTTPS"** کلیک کنید
2. مطمئن شوید که **"Force HTTPS"** فعال است

---

## 🎉 تبریک!

پروژه شما با موفقیت دیپلوی شد! حالا می‌توانید لینک سایت را با دیگران به اشتراک بگذارید.

---

## ❓ رفع مشکلات رایج

### خطا: "DATABASE_URL is not defined"

**راه‌حل:**
1. به **Site settings** > **Environment variables** بروید
2. مطمئن شوید `DATABASE_URL` درست تنظیم شده است
3. روی **"Trigger deploy"** > **"Clear cache and deploy site"** کلیک کنید

### خطا: "Build failed"

**راه‌حل:**
1. لاگ build را بخوانید
2. معمولاً مشکل از نصب نشدن پکیج‌ها است
3. مطمئن شوید `package.json` کامل است

### سایت باز می‌شود اما API کار نمی‌کند

**راه‌حل:**
1. بررسی کنید که `netlify.toml` درست ساخته شده باشد
2. مطمئن شوید redirects درست تنظیم شده‌اند
3. لاگ Functions را در Netlify چک کنید

### دیتابیس خالی است

**راه‌حل:**
1. مطمئن شوید `npm run db:push` را اجرا کرده‌اید
2. بررسی کنید که `DATABASE_URL` درست است
3. در Supabase، از **SQL Editor** استفاده کنید تا جداول را ببینید

---

## 📞 کمک بیشتر

اگر در هر مرحله‌ای به مشکل خوردید:
1. پیام خطا را کامل کپی کنید
2. به من بگویید در کدام مرحله هستید
3. اسکرین‌شات از خطا بگیرید

موفق باشید! 🚀
